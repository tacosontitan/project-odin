import { ScheduledWorkflowProps } from "./ScheduledWorkflowProps"

export const ScheduledWorkflow: React.FC<ScheduledWorkflowProps> = (props: ScheduledWorkflowProps) => {

    return (
        <></>
    );

}